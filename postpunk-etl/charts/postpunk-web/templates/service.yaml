apiVersion: v1
kind: Service
metadata:
  name: {{ include "postpunk-web.fullname" . }}
  labels:
    {{- include "postpunk-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postpunk-web.selectorLabels" . | nindent 4 }}